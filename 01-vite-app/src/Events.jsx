import React from "react";

const Events = () => {
  return (
    <div>
      <button type="button" onClick={() => console.log(23 + 45)}>
        When Button Click On
      </button>
    </div>
  );
};

export default Events;
